#include "main.h"
#include "bot.h"

bool ChuckerViewBot::doExecuteHTTPRequest(const char* url, int req, bool bUseProxy, const char* proxy_host, int proxy_port)
{
    LOG("ChuckerViewBot::doExecuteHTTPRequest(%s, %d, %d, %s, %d)", url, req, bUseProxy, proxy_host, proxy_port);

    // TODO

    return true;
}